version: 1
mode: project

modules:
  base-harness: enabled
  context-graph: enabled
  spec-driven: enabled
  bdd: enabled
  feedback-loop: enabled
  agent-runtime: disabled
  shared-knowledge: disabled

context:
  current: ./docs
  retrieval: mq

bdd:
  framework: pytest-bdd
  command: make test-bdd
  features: ./test/bdd/features
