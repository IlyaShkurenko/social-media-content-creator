# 004-content-aware-subtitles

- Kind: `experiment`
- Scenario: `mixed-media-stock-baseline-001`
- Hypothesis: Reserving a product-safe caption zone and suppressing duplicate CTA subtitles removes visual false positives without changing the hook or storyboard intent.

## Result

`failed_before_evaluation`

The experiment runner allocated this immutable ID, then failed because the new artifact-snapshot branch referenced a missing `REPO_ROOT` constant. No metric was produced and no paid provider was called. The runner bug was fixed; the creative experiment continues under the next experiment ID so this failed attempt is never silently reused.
